# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
#default: &default
#  adapter: sqlite3
#  pool: 5
#  timeout: 5000

development:
  adapter: mysql
  encoding: utf8
  database: honeymoon_development
  username: root
  password:
  socket: /tmp/mysql.sock
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  encoding: utf8
  database: honeymoon_test
  username: root
  password:
  socket: /tmp/mysql.sock
  host: localhost

# production:
#   adapter: mysql
#   encoding: utf8
#   database: honeymoon_production
#   username: root
#   password:
#   socket: /tmp/mysql.sock
<div class="row">
    <% @destinations.each do |destination| %>
        <div class="col-sm-4">
            <h3><%= destination.name %></h3>

            <% destination.islands.each do |island| %>
                <%= link_to island.name, island_path(island) %>
            <% end %>

            <% destination.hotels.each do |hotel| %>
              <%= link_to hotel.name, hotel_path(hotel) %>
            <% end %>

        </div>
    <% end %>
</div>